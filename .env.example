# Horizon House — Supabase (realtime multi-team workshops)
# Copy to ".env" and fill in the publishable/anon key from your Supabase project:
#   Project Settings → API → Project API keys → anon / publishable
#
# Without these set, the app runs in local-only mode (single device + practice
# teams) — no realtime, no shared scoreboard.

VITE_SUPABASE_URL=https://vsxgesmejxkiqwqsaeyi.supabase.co
VITE_SUPABASE_ANON_KEY=
